This task is to implement patching functionality. This means completing the `TicketStoreClient::update` method and handling the `Command::Update` in the `server` function.

Follow the `TODO` comments in the code.