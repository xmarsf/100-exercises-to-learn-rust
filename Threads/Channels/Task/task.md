This task is to define the `Command::Insert` variant and implement the `server` function. 
As guided by the `TODO` comments, the server should loop indefinitely, processing commands from its receiver.